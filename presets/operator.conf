# operator — el codigo tal como aparece en los titulos de la primera pelicula
# y en las pantallas de los operadores: plano, apretado, sin gradiente, con
# ripples cuadrados ocasionales.
#
# version= ya trae de upstream (js/config.js:154): numColumns=108, fallSpeed=0.6,
# cycleSpeed=0.01, bloomSize=0.6, rippleTypeName=box, raindropLength=1.5 y su
# paleta verde. Lo de abajo solo agrega lo propio de un screensaver.

version=operator
suppressWarnings=true
